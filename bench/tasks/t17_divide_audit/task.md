这个仓库有 6 个互不相关的包：`pkg_alpha` … `pkg_zeta`。每个包里恰好有一处政策违规。

政策（每个包各中一条，不重复）：
1. 禁止 `eval` / `exec`
2. 禁止可变对象做默认参数
3. 禁止用 `is` 比较字符串内容
4. 禁止用 `==` 比较浮点计算结果
5. 禁止裸 `except:`
6. 禁止用 `x or []` 这类对假值不友好的默认

请分别审计每个包，把发现写进 `AUDIT.md`。格式每行一条，精确为：

```
pkg_alpha: <一句话>
pkg_beta: <一句话>
pkg_gamma: <一句话>
pkg_delta: <一句话>
pkg_epsilon: <一句话>
pkg_zeta: <一句话>
```

六个包彼此没有调用关系。不要把六个包的源码都堆进你自己的上下文。
不许修改任何 `test_` 文件，也不许改被审计的源码 —— 这题只要报告。
