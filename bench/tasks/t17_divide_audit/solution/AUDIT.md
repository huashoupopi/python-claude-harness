pkg_alpha: parse_expr calls eval
pkg_beta: add_tag uses a mutable default list
pkg_gamma: is_live compares strings with is
pkg_delta: almost_one compares floats with ==
pkg_epsilon: read_text has a bare except
pkg_zeta: with_defaults uses opts or {}
