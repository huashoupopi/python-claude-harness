# D4 核实：裁剪是否按 tool-call / tool-result 组切

> 2026-08-20。包 D 要求先核实再动手。结论：**已经做对，不重写。**

设计文档 ② 第 4 条要的是：裁中间历史时不得把 `tool_calls` 和对应 `tool` 结果切断（切断下一轮 API 400）。

## 已有保护（三处，同一形状）

1. **`snip_compact`（L1）** `examples/22_trunk.py:679-684`
   - 头：若保留区最后一条是 tool_use，while 把紧跟的 tool 结果一并纳入（:679-681）
   - 尾：若裁切点落在 tool 结果上，while 退到发起该调用的 assistant（:682-684）
2. **`reactive_compact`** `:791-793`  
   留尾约 5 条时，同样：切点在 tool 结果上就往回退到 tool_use。
3. **teammate 历史截断** `:1944-1957`  
   2026-08-15 已从裸切 `messages[-20:]` 改成同一套「退到 assistant」；主循环当时已有保护，这条线补过。

## 已有测试（覆盖切断会 400 的形状）

- `tests/test_compact.py::test_head_boundary_cut`：L1 头部边界天然落在 call/result 中间，断言零孤儿
- `tests/test_compact.py::test_long_history_snipped`：长历史确实被裁，头 3 条保留
- `tests/test_compact.py::test_compact_plus_other_tools_leaves_no_orphan`：同一轮 compact + 别的工具，被跳过的调用必须有回复
- `tests/test_compact.py::test_teammate_truncation_keeps_pairs`：切口故意落在 tool 上，改后无孤儿

## 本次动作

不改上述函数，不另写一套「按轮切」。L1 `max_msgs=50` 未动。
