# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv


.env

# pytest 缓存
.pytest_cache/

# === Harness 运行时产物:每次跑都会重新生成,不进仓 ===
# 任务系统 / 定时任务 / 团队收件箱 / worktree 隔离目录
.tasks/
.scheduled_tasks.json
.mailboxes/
.worktrees/
# 记忆系统 / 轨迹归档 / 后台任务输出
.memory/
.transcripts/
.traces/
.task_outputs/
# mini-bench 的历次跑分结果(题库 bench/tasks/ 要进仓,成绩单不进)
bench/runs/

# === 学习用的一次性演示脚本(不是 harness 的一部分) ===
copy_demo.py


# bench 考场产物(__pycache__/.pytest_cache 上面已有)
# ⚠️ .gitignore 挡不住那个判分 bug —— 它只管 git 追不追,不管文件在不在磁盘上;
#    真正的修复是 copytree(ignore=IGNORE_CACHES),别把这两件事混为一谈。
bench/runs/
.bench_trace.json
